[Unit]
Description=Minecraft dedicated server
Wants=network.target
After=network.target

[Service]
ExecStart={{ mc_install_dir }}/start_minecraft.sh
WorkingDirectory={{ mc_install_dir }}

[Install]
WantedBy=multi-user.target
